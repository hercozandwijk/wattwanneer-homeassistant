[pytest]
asyncio_mode = auto
testpaths = tests
# Zonder dit vindt pytest in CI de map custom_components niet; lokaal werkt het
# toevallig wel omdat de repo-root dan al op sys.path staat.
pythonpath = .
